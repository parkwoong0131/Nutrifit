const lunch = document.getElementById('nutritionLunch');


function paintData(newTodo) {
    localStorage.getItem('todos');
    const li = document.lunch.createElement("li");
    li.id = todos.id;
    const span = document.lunch.createElement("span");
    span.innerText = todos.text;
    const button = document.lunch.createElement("button");
    button.innerText = "💊";
  }
